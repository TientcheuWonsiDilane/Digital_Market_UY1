<% content_for :title, "New Product" %>

<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="bg-white rounded-3xl border border-emerald-100 p-8 shadow-sm">
    <div class="mb-8">
      <h1 class="text-3xl font-black text-emerald-900">Publish a Product</h1>
      <p class="text-sm text-gray-500 mt-1">List your item for sale. Students on campus are waiting!</p>
    </div>
    <%= render "form", item: @item %>
  </div>
</div>
